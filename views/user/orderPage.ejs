<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Orders - Rube Collection</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Lato:wght@300;400&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: "Lato", sans-serif;
        }
    </style>
</head>
<body class="bg-gray-50">
    <%- include("../partials/user/header.ejs") %>
    
    <div class="max-w-full mx-auto px-6 lg:px-12 py-8">
        <div class="flex flex-col lg:flex-row gap-8">
            <%- include("../partials/user/profileSidebar.ejs") %>
            
            <div class="flex-1">
                <div class="bg-gray-100 rounded-lg p-6 lg:p-8">
                    <div class="bg-white rounded-lg shadow-sm p-6 lg:p-10">
                        <h1 class="text-3xl font-semibold text-gray-800 mb-8">My Orders</h1>
                        
                        <% if (order && order.length > 0) { %>
                            <div class="space-y-6">
                                <% order.forEach((ord, index) => { %>
                                    <div class="bg-white rounded-lg shadow-sm border border-gray-200">
                                        <div class="p-6 border-b border-gray-200">
                                            <div class="flex flex-col md:flex-row md:items-center md:justify-between">
                                                <div class="mb-4 md:mb-0">
                                                    <h3 class="text-lg font-semibold text-gray-800">
                                                        Order #<%= ord._id.toString().slice(-8).toUpperCase() %>
                                                    </h3>
                                                    <p class="text-gray-600 text-sm mt-1">
                                                        Placed on: <%= ord.createdAt ? new Date(ord.createdAt).toLocaleDateString('en-IN') : 'N/A' %>
                                                    </p>
                                                </div>
                                                <div class="flex flex-col items-start md:items-end">
                                                    <p class="text-lg font-bold text-gray-800">₹<%= ord.totalPrice || ord.finalAmount %></p>
                                                    <span class="px-3 py-1 rounded-full text-sm font-medium mt-1
                                                        <%= ord.orderedItem && ord.orderedItem[0]?.status === 'Shipped' ? 'bg-green-100 text-green-800' : 
                                                           ord.orderedItem && ord.orderedItem[0]?.status === 'Processing' ? 'bg-blue-100 text-blue-800' : 
                                                           'bg-yellow-100 text-yellow-800' %>">
                                                        <%= ord.orderedItem && ord.orderedItem[0]?.status || 'Pending' %>
                                                    </span>
                                                </div>
                                            </div>
                                        </div>
                                        
                                        <% if (ord.orderedItem && ord.orderedItem.length > 0) { %>
                                            <div class="p-6">
                                                <h4 class="font-medium text-gray-700 mb-4">Order Items</h4>
                                                <div class="space-y-4">
                                                    <% ord.orderedItem.forEach((item, itemIndex) => { %>
                                                        <div class="flex items-center gap-4 p-3 bg-gray-50 rounded-lg">
                                                            <div class="w-16 h-16 bg-gray-200 rounded-lg flex items-center justify-center">
                                                                <span class="text-gray-500 text-xs">Image</span>
                                                            </div>
                                                            <div class="flex-1">
                                                                <p class="font-medium text-gray-800">
                                                                    Product: <%= item.productId ? item.productId.toString().slice(-6).toUpperCase() : 'N/A' %>
                                                                </p>
                                                                <div class="flex flex-wrap gap-4 mt-1 text-sm text-gray-600">
                                                                    <span>Quantity: <%= item.quantity || 1 %></span>
                                                                    <span>Price: ₹<%= item.price || 'N/A' %></span>
                                                                    <span>Size: <%= item.ml || 'N/A' %></span>
                                                                </div>
                                                            </div>
                                                            <div class="text-right">
                                                                <span class="px-2 py-1 rounded text-xs font-medium
                                                                    <%= item.status === 'Shipped' ? 'bg-green-100 text-green-800' : 
                                                                       item.status === 'Processing' ? 'bg-blue-100 text-blue-800' : 
                                                                       'bg-yellow-100 text-yellow-800' %>">
                                                                    <%= item.status || 'Pending' %>
                                                                </span>
                                                            </div>
                                                        </div>
                                                    <% }); %>
                                                </div>
                                            </div>
                                        <% } %>
                                        
                                        <div class="p-6 bg-gray-50 rounded-b-lg">
                                            <div class="flex flex-col md:flex-row md:items-center md:justify-between text-sm">
                                                <div class="text-gray-600 mb-2 md:mb-0">
                                                    <p><strong>Payment:</strong> <%= ord.payment || 'N/A' %></p>
                                                    <p><strong>Payment Status:</strong> 
                                                        <span class="<%= ord.paymentStatus === 'Done' ? 'text-green-600' : 'text-yellow-600' %>">
                                                            <%= ord.paymentStatus || 'Pending' %>
                                                        </span>
                                                    </p>
                                                </div>
                                                <div class="flex gap-3">
                                                    <button class="px-4 py-2 bg-red-700 text-white rounded-md hover:bg-red-800 transition text-sm">
                                                        View Details
                                                    </button>
                                                    <button class="px-4 py-2 border border-gray-300 text-gray-700 rounded-md hover:bg-gray-50 transition text-sm">
                                                        Track Order
                                                    </button>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                <% }); %>
                            </div>
                        <% } else { %>
                            <div class="bg-white rounded-lg shadow-sm p-12 text-center">
                                <svg class="w-24 h-24 mx-auto text-gray-300 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"/>
                                </svg>
                                <h3 class="text-xl font-medium text-gray-800 mb-2">No orders yet</h3>
                                <p class="text-gray-500 mb-6">Your order history will appear here once you place an order</p>
                                <a href="/shop" class="inline-block px-6 py-3 bg-red-700 text-white rounded-md hover:bg-red-800 transition">
                                    Start Shopping
                                </a>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <%- include("../partials/user/footer.ejs") %>
</body>
</html>